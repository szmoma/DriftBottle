<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/lockscreen_bg"
    android:paddingLeft="10dip"
    android:paddingRight="10dip" >

    <TextView
        android:id="@id/tv_title"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerInParent="true"
        android:layout_marginTop="10dp"
        android:padding="8dp"
        android:gravity="center"
        android:text="@string/tip_gesture_local_head"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:visibility="visible" />

    <TextView
        android:id="@+id/tvNoSetPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:paddingLeft="15dip"
        android:paddingRight="15dip"
        android:layout_marginTop="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="20dp"
        android:padding="8dp"
        android:text="@string/tip_gesture_local_unforget"
        android:textColor="@android:color/white" />

    <com.hnmoma.driftbottle.custom.LocusPassWordView
        android:id="@+id/mLocusPassWordView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@id/tvNoSetPassword"
        android:layout_below="@id/tv_title" >
    </com.hnmoma.driftbottle.custom.LocusPassWordView>

</RelativeLayout>